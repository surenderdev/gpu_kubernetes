"# resume parser baseline opensource model one" 

Run the following command to start the FastAPI application using Uvicorn:
```bash
uvicorn app:app

Access Swagger UI at:
http://localhost:8000/docs
# opensource_ollama
